{
  "name": "quickhire",
  "private": true,
  "scripts": {
     "dev": "concurrently -k -n FRONTEND -c green \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}